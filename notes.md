# to install grafana-k8s-monitoring
```bash
helm install grafana-k8s-monitoring grafana/k8s-monitoring --namespace default --create-namespace --values values.yaml
```

# to compile the project use
``` bash
npx tsc
```
